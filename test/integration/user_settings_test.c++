#include "integration_common.h++"

SCENARIO_METHOD(IntegrationTest, "user settings", "[integration][user_settings]") {

  GIVEN("placeholder") {
    WHEN("placeholder") {
      THEN("placeholder") {
        REQUIRE(2 + 2 == 4);
      }
    }
  }
}